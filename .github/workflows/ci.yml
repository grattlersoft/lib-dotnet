name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Setup NuGet
        uses: NuGet/setup-nuget@v4

      - name: Restore
        run: nuget restore src/SharedDotnet.slnx

      - name: Build
        run: msbuild src/SharedDotnet.slnx /p:Configuration=Release /v:minimal
